services:
  strom-controller:
    build:
      context: ..
      dockerfile: controller/Dockerfile
      args:
        VERSION: dev
    image: strom-controller:dev
    ports:
      - "9000:9000"
    volumes:
      - strom-controller-data:/data
    restart: unless-stopped

volumes:
  strom-controller-data:
